# LLVM IR գեներացիայի աշխատանքային պլան

## Նպատակը

Սեմանտիկորեն վավեր `Program` AST-ից կառուցել ստուգված LLVM IR։ Կոդի
գեներատորը անուններ կամ տիպեր նորից չի լուծելու․ այն օգտագործելու է
`SymbolTable`-ի և `SemanticModel`-ի արդեն հաշվարկած տվյալները։ Սխալ AST-ը
գեներատորին փոխանցելը համարվելու է ծրագրավորման սխալ։

Առաջին նպատակը ընթեռնելի `.ll` ստանալն է։ Օբյեկտային ֆայլի ստեղծումն ու
գործարկումը կավելացվեն միայն IR գեներացիայի ավարտից հետո։ Ամեն փուլ պետք է
ավարտվի աշխատող build-ով և նույն միասնական Catch2 executable-ի թեստերով։

## Repository-ի ներկա վիճակը

LLVM IR գեներատորը դեռ իրականացված չէ, և CMake-ը LLVM չի գտնում կամ կապում։
LLVM աշխատանքը սկսվում է պատրաստ frontend-ի վրա․

- parser-ը կառուցում է ամբողջ լեզուն ներկայացնող `Program` AST,
- `SemanticAnalyzer`-ը լուծում է անունները և տիպերը, ստուգում է կանչերը,
  control flow-ն ու `RETURN`-ը,
- `SemanticModel`-ը AST հանգույցները կապում է `SymbolId`-ների ու տիպերի հետ և
  պահում է entry point-ը,
- `avium-runtime`-ը արդեն կառուցվում է որպես առանձին C17 static library և
  իրականացնում է տեքստերը, զանգվածները, I/O-ն, `NUM`, `STR`, `SQR` ու runtime
  սխալները։

Այսպիսով պլանի անմիջական հաջորդ քայլը LLVM-ի միացումն ու նվազագույն LLVM
backend-ի ստեղծումն է։ Այն պետք է օգտագործի `SemanticModel`-ի և runtime C
ABI-ի պայմանագրերը։

## LLVM ներկայացման նախնական պայմանավորվածությունները

| Կեռաս | LLVM IR |
| --- | --- |
| `BOOL` | `i1` |
| `REAL` | `double` |
| `TEXT` | runtime-ի `avium_text`-ին ABI-compatible կառուցվածք՝ `{ ptr, size_t, i1 }` |
| պրոցեդուրա | `void` վերադարձնող ֆունկցիա |
| ֆունկցիա | համապատասխան scalar տիպ վերադարձնող ֆունկցիա |
| զանգված | opaque runtime descriptor-ի `ptr` (`avium_array*`) |

Պարզ պարամետրերը, ներառյալ `TEXT` կառուցվածքը, փոխանցվելու են արժեքով,
զանգվածները՝ descriptor-ի հղումով։ `size_t`-ի LLVM տիպը պետք է վերցնել module-ի
target data layout-ից և hard-code չանել որպես `i64`։
Օգտատիրոջ `Main` ենթածրագրի կողքին կստեղծվի C ABI-ի `i32 @main()` wrapper,
որը կանչում է այն և վերադարձնում `0`։ Օգտատիրոջ անունները պետք է mangling
ունենան, որպեսզի չբախվեն runtime-ի և համակարգային անուններին։

Տեղային scalar փոփոխականների հիշողությունը կհատկացվի ենթածրագրի entry
block-ում և կսկզբնարժեքավորվի Կեռասի կանոններով՝ `FALSE`, `0.0`, դատարկ տեքստ։
Ֆունկցիայի `RETURN` հրամանը կհաշվարկի իր արտահայտությունը և կստեղծի անմիջական
`ret`։ Հաջող semantic analysis-ից հետո ֆունկցիայի բոլոր հնարավոր կատարման
ուղիները երաշխավորված ավարտվում են `RETURN`-ով։

## Կանոններ

- Կոդի գեներատորին փոխանցվում է միայն հաջող semantic analysis անցած `Program`-ը՝
  հենց այն `SymbolTable`-ի և `SemanticModel`-ի հետ, որոնցով այն վերլուծվել է։
- Անունների լուծումը, տիպերի ստուգումը, `Main`-ի գոյությունը և ստորագրությունների
  վավերությունը semantic analyzer-ի պատասխանատվությունն են։ Codegen-ը դրանք չի
  կրկնում և Կեռասի semantic diagnostic չի ստեղծում։
- `ScalarType::Name`-ը պարունակում է միայն `Bool`, `Real` և `Text` արժեքները։ Հաջող
  semantic analysis-ից հետո յուրաքանչյուր օգտագործվող արտահայտություն ունի
  կոնկրետ տիպ, իսկ անհնար enum ճյուղերը նշվում են `std::unreachable()`-ով։
- Parser-ը առանց տիպի `DIM` կամ պարամետր չի ավելացնում AST-ին, ուստի codegen-ը
  դրանց տիպի գոյությունը չի ստուգում։
- AST հանգույցները, փոփոխականների storage-ները և կանչվող ենթածրագրերը կապվում են
  `SymbolId`-ներով։ Codegen-ը դրանց անուններով որոնում չի
  կատարում։
- Semantic analyzer-ը `SemanticModel`-ում պահում է entry-point-ի `SymbolId`-ն։
  Codegen-ը AST-ում `Main` անունով ենթածրագիր նորից չի որոնում։
- `SemanticModel`-ի պարտադիր կապերը codegen-ում նախապայման են։ Դրանց բացակայության
  համար պաշտպանական semantic ստուգումներ չեն ավելացվում։
- Array/scalar և function/procedure պայմանները semantic ստուգումներ չեն․ դրանք
  ընտրում են արդեն վավեր հանգույցի LLVM ներկայացման ձևը։
- `llvm::verifyModule()`-ը պարտադիր է և ստուգում է գեներատորի արտադրած LLVM IR-ը։
  Դրա ձախողումը compiler-ի ներքին սխալ է, ոչ թե աղբյուր ծրագրի diagnostic։
- Առայժմ հաջող pipeline-ից հետո ամբողջ LLVM IR-ը տպվում է `stdout`։ Սա ժամանակավոր
  վարք է մինչև CLI-ի ելքային ռեժիմների իրականացումը։

## Փուլ 1․ LLVM-ի միացում և արտաքին ինտերֆեյս

- CMake-ում գտնել և կապել LLVM-ի անհրաժեշտ բաղադրիչները՝ առանց առանձին
  executable-ներ ստեղծելու։
- Ավելացնել `codegen.hxx` և `codegen.cxx`։
- Սահմանել `CodeGenerator`, որն ընդունում է `LLVMContext`, `SymbolTable` և
  `SemanticModel`, իսկ `generate()`-ով վերադարձնում է `Module`։
- Ամեն գեներացումից հետո գործարկել `llvm::verifyModule()`։ Verification-ի
  ձախողումը պետք է երևա որպես compiler-ի ներքին սխալ, ոչ թե Կեռասի diagnostic։
- Առաջին թեստով դատարկ `Main`-ից ստանալ վավեր module և entry point։
- LLVM dependency-ն պահել միայն LLVM backend-ի թիրախներում, որպեսզի
  `avium-runtime`-ը շարունակի ինքնուրույն C17 գրադարան կառուցվել։

## Փուլ 2․ տիպեր, ստորագրություններ և գեներատորի վիճակ

- Մեկ տեղում իրականացնել `ScalarType::Name -> llvm::Type` փոխակերպումը և
  առանձին սահմանել `avium_text`/`avium_array*` ABI ներկայացումները։
- Նախապես հայտարարել ծրագրի բոլոր ենթածրագրերը, որպեսզի աշխատեն առաջ ուղղված
  և ռեկուրսիվ կանչերը։
- Պահել `SymbolId -> llvm::Function*` և `SymbolId -> storage` քարտեզները։ AST-ի
  տեքստային անունները storage որոնելու համար չօգտագործել։
- C ABI-ի wrapper-ը կապել `SemanticModel`-ում պահված entry-point `SymbolId`-ին՝
  առանց `Main` անունով կրկնակի որոնման։
- Յուրաքանչյուր ենթածրագրի համար ստեղծել entry block, տեղային storage-ներ և
  պարամետրերի սկզբնական store-ներ։ Պրոցեդուրայի ավարտին ստեղծել `ret void`, իսկ
  ֆունկցիայի յուրաքանչյուր `RETURN`-ի համար՝ համապատասխան արժեքով `ret`։
- Թեստերով ստուգել պրոցեդուրան, scalar պարամետրերով ֆունկցիան, առաջ ուղղված
  հայտարարումը և ռեկուրսիվ ստորագրությունը։

## Փուլ 3․ scalar արժեքներ և վերագրումներ

- Գեներացնել `BOOL`, `REAL` և տեքստային լիտերալները։ Տեքստային լիտերալները
  պահել module-ի private constant տվյալներում և runtime handle դարձնել։
- Իրականացնել scalar `Variable` load-ը և `LET` store-ը։
- Իրականացնել ունար `+`, `-`, `NOT` գործողությունները։
- Իրականացնել թվաբանական գործողությունները։ `/`-ը սովորական floating-point
  բաժանում է, `\`-ը՝ դեպի զրո կլորացված քանորդ, `MOD`-ը՝ մնացորդ, `^`-ը՝
  runtime/libm `pow` կանչ։
- Իրականացնել թվային և տրամաբանական հավասարություններն ու համեմատությունները։
- Յուրաքանչյուր ենթափուլի թեստը պետք է և՛ verify անի module-ը, և՛ ստուգի
  արդյունքի հիմնական instruction-ները։

## Փուլ 4․ ղեկավարող կառուցվածքներ

- `IF`/`ELSEIF`/`ELSE`-ի համար կառուցել condition, branch և merge basic
  block-եր։ Դատարկ ճյուղերը նույնպես պետք է ճիշտ ավարտվեն, իսկ `RETURN`-ով
  ավարտված ճյուղին երկրորդ terminator չպետք է ավելացվի։
- `WHILE`-ի համար կառուցել condition, body և exit block-եր։
- `AND` և `OR` գործողությունները գեներացնել կարճ հաշվարկմամբ՝ branch-երով և
  PHI արժեքով, ոչ թե սովորական bitwise instruction-ով։
- `FOR`-ի begin, end և step արտահայտությունները հաշվարկել ճիշտ մեկ անգամ։
  Դրական քայլի դեպքում կիրառել `<=`, բացասականի դեպքում՝ `>=`, ապա մարմնից
  հետո մեծացնել հաշվիչը։
- Թեստավորել nested control flow-ը և այն դեպքերը, երբ ճյուղը կամ ցիկլի մարմինը
  վերջին statement-ն է ենթածրագրում։

## Փուլ 5․ օգտատիրոջ ենթածրագրերի կանչեր

- `CALL`-ը գեներացնել որպես `void` կանչ, `Apply`-ը՝ որպես արժեք վերադարձնող
  կանչ։ Կանչվող ֆունկցիան գտնել `SemanticModel`-ի `SymbolId`-ով։
- Scalar արգումենտները փոխանցել արժեքով։
- Ապահովել ռեկուրսիվ և փոխադարձ կանչերը։
- Թեստավորել բազմաթիվ արգումենտներ, վերադարձվող արժեքը և ֆունկցիայի արդյունքի
  անմիջական օգտագործումը ավելի մեծ արտահայտության մեջ։

## Փուլ 6․ runtime և `TEXT`

- Օգտագործել `runtime/include/*.h`-ում արդեն սահմանված C ABI-ն՝ տեքստի
  ստեղծման, պատճենման, տեղափոխման, ոչնչացման, միակցման, համեմատման և
  ներմուծման համար։ LLVM հայտարարությունները պետք է ճշգրիտ ABI-compatible
  լինեն այդ header-ների հետ։
- `&`, տեքստային `=`, `<>`, `<`, `<=`, `>` և `>=` գործողությունները իջեցնել
  runtime կանչերի։ Համեմատությունը պետք է լինի բովանդակությամբ, ոչ թե pointer-ով։
- Նույն ABI-ով կանչել արդեն իրականացված `Input`, `NUM`, `STR`, `LEN` և `SQR`
  գործողությունները՝ փոխանցելով source line-ը բոլոր այն ֆունկցիաներին, որոնց
  ստորագրությունը դա պահանջում է։
- Generic `Print` ստորագրությունը backend-ում մասնագիտացնել փաստացի արգումենտի
  տիպով՝ BOOL, REAL կամ TEXT runtime ֆունկցիայի կանչի։
- Պահպանել `runtime/design.md`-ի ownership պայմանագիրը․ լիտերալները borrowed
  են, copy/concat/Input/STR արդյունքները՝ owned, move-assign-ը փոխանցում է
  սեփականությունը, իսկ ամեն owned տեղային կամ ժամանակավոր արժեք ոչնչացվում է
  ճիշտ մեկ անգամ։
- Runtime-ի դեռ դատարկ `runtime/tests/` բաժնում ավելացնել ABI-ի, սահմանային
  արժեքների և սխալ ելքերի թեստեր։

## Փուլ 7․ զանգվածներ և runtime ստուգումներ

- Տեղային զանգվածի `DIM`-ի ժամանակ չափի արտահայտությունը հաշվարկել ճիշտ մեկ
  անգամ և կանչել `avium_array_create`-ը՝ տարրի tag-ով ու source line-ով։ Չափի
  ամբողջ/դրական/ներկայացնելի լինելը, allocation-ը և տարրերի սկզբնարժեքավորումը
  թողնել արդեն այդ պայմանագիրն իրականացնող runtime-ին։
- `a[i]`-ի հասցեն ստանալ համապատասխան `avium_text_array_at`,
  `avium_real_array_at` կամ `avium_bool_array_at` կանչով։ Այդ runtime կանչերն
  են ստուգում descriptor-ը, տարրի տիպը, ինդեքսի ամբողջ լինելն ու սահմանները։
- Ենթածրագրից դուրս գալու բոլոր ուղիներում տեղային զանգվածների համար կանչել
  `avium_array_destroy`, իսկ զանգվածային պարամետրերը չոչնչացնել։
- Զանգվածային պարամետրերը փոխանցել հղումով, որպեսզի ենթածրագիրը կարողանա փոխել
  կանչողի տարրերը։
- Թեստավորել դինամիկ չափը, տարբեր բազային տիպերը, ենթածրագրին փոխանցումը և
  չափի/ինդեքսի runtime սխալները։

## Փուլ 8․ հրամանային տող և ամբողջական ստուգում

- CLI-ին ավելացնել LLVM IR արտածելու հստակ ռեժիմ, օրինակ՝ `--emit-llvm`, և
  `-o` ելքային ֆայլի ընտրություն։ Մինչ այդ հաջող codegen-ի LLVM IR-ը տպել
  `stdout`։
- Compiler pipeline-ը դարձնել՝ scanner → parser → semantic analyzer → code
  generator → verifier → IR output։ Parse կամ semantic սխալի դեպքում codegen
  չգործարկել։
- Բոլոր `examples/*.bas` ֆայլերի համար ավելացնել IR smoke test։ Ընտրված փոքր
  ծրագրերը նաև իրականացնել LLVM interpreter/JIT-ով կամ native executable-ով և
  ստուգել stdout-ը։ Քանի որ օրինակների մի մասը դիտավորյալ semantic սխալ է,
  նախ դրանք բաժանել expected-success և expected-diagnostic խմբերի։
- LLVM-ի տարբեր տարբերակների պատճառով ամբողջ IR-ի մեծ snapshot-ներից խուսափել։
  Նախընտրել `verifyModule`, կառուցվածքային ստուգումներ և փոքր, կայուն IR
  հատվածներ։

## Ավարտված լինելու չափանիշները

Պլանը ավարտված է, երբ Կեռասի սահմանման բոլոր գործողությունները, ղեկավարող
կառուցվածքները, ենթածրագրերը, ներդրված ֆունկցիաները և զանգվածները ստանում են
վավեր LLVM IR, բոլոր օրինակները հասնում են codegen փուլ, runtime սխալներն ունեն
կանխատեսելի վարք, իսկ ամբողջ Catch2/CTest փաթեթն անցնում է մեկ executable-ով։
